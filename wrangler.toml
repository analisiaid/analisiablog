# Cloudflare Pages project config
# Build: hugo && npx pagefind --site public
# Deploy: npx wrangler pages deploy ./public --project-name analisiablog

name = "analisiablog"
pages_build_output_dir = "public"
compatibility_date = "2026-06-01"
